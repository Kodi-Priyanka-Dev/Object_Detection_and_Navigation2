SYSTEM ARCHITECTURE & COMPONENT DIAGRAMS
═════════════════════════════════════════════════════════════════════════════

This document provides visual representations of the AI Navigation System
architecture and data flows.


1. HIGH-LEVEL SYSTEM ARCHITECTURE
═════════════════════════════════════════════════════════════════════════════

                         ┌─────────────────┐
                         │   User (Phone)  │
                         └────────┬────────┘
                                  │
                         ┌────────▼────────┐
                    ┌────┤ FLUTTER APP     │◄────┐
                    │    │ (Vivo V2303)    │     │
                    │    └────────┬────────┘     │
                    │             │              │
         ┌──────────▼─────┐      │         ┌─────▼────────┐
         │ Camera Input   │      │         │ Sensor Input │
         │ (frames)       │      │         │ (compass)    │
         └────────┬───────┘      │         └──────────────┘
                  │              │
                  │ JPEG/BASE64  │
                  │              │
                  └──────┬───────┘
                         │
         ┌───────────────▼──────────────────┐
         │  FLASK BACKEND SERVICE (5000)    │
         │  (PC/Server on WiFi)             │
         └───────────────┬──────────────────┘
                         │
         ┌───────────────▼──────────────────┐
         │  YOLO MODEL (detect1892)         │
         │  (20 classes, 88.87% mAP)        │
         └───────────────┬──────────────────┘
                         │
         ┌───────────────▼──────────────────┐
         │  JSON RESPONSE                   │
         │  (detections, directions)        │
         └───────────────┬──────────────────┘
                         │
                  ┌──────▼──────┐
                  │ App Display │
                  │ + Voice TTS │
                  └─────────────┘


2. DATA FLOW DIAGRAM
═════════════════════════════════════════════════════════════════════════════

INPUT SOURCES:
┌────────────────────────────────────────────────────────────────────────┐
│                                                                        │
│  📷 CAMERA                          🧭 SENSORS                        │
│  ├─ RGB frames (640×480)            ├─ Magnetometer (heading)         │
│  ├─ 30 FPS                          ├─ Accelerometer (tilt)           │
│  └─ JPEG compressed                 └─ Gyroscope (rotation)           │
│                                                                        │
└─┬──────────────────────────────────────────────────────────────────┬──┘
  │                                                                  │
  │ Camera → Preprocessing → Detection                              │
  │                                                                  │
┌─▼──────────────────────────────────────────────────────────────────▼──┐
│                   YOLO MODEL (detect1892)                             │
│  ┌──────────────────────────────────────────────────────────────┐    │
│  │ Input: 640×480 RGB frame                                     │    │
│  │ Process: CNN feature extraction + detection head             │    │
│  │ Output: Bounding boxes + class labels + confidence scores    │    │
│  └──────────────────────────────────────────────────────────────┘    │
└─┬──────────────────────────────────────────────────────────────────┬──┘
  │                                                                  │
  │ Raw Detections → Confidence Filtering                           │
  │                                                                  │
┌─▼──────────────────────────────────────────────────────────────────▼──┐
│                   CONFIDENCE THRESHOLDING                             │
│  ├─ Door: > 10% ✓                                                    │
│  ├─ Human: > 50% ✓                                                   │
│  ├─ Table: > 5% ✓                                                    │
│  └─ Other: > 5% ✓                                                    │
└─┬──────────────────────────────────────────────────────────────────┬──┘
  │                                                                  │
  │ Filtered Detections → Distance Calculation                      │
  │                                                                  │
┌─▼──────────────────────────────────────────────────────────────────▼──┐
│                      DISTANCE ESTIMATION                             │
│  Distance(cm) = Reference(100px) × 100 / Box Height               │
└─┬──────────────────────────────────────────────────────────────────┬──┘
  │                                                                  │
  │ Detections + Distance → Navigation Logic                        │
  │                                                                  │
┌─▼──────────────────────────────────────────────────────────────────▼──┐
│                   NAVIGATION DECISION                                │
│  ├─ Door Center - Screen Center = offset                           │
│  ├─ If offset < -50: Direction = LEFT                             │
│  ├─ If offset > +50: Direction = RIGHT                            │
│  └─ Else: Direction = FORWARD                                     │
└─┬──────────────────────────────────────────────────────────────────┬──┘
  │                                                                  │
  └──────────────────────┬─────────────────────────────────────────┘
                         │
OUTPUT RESPONSES:
┌────────────────────────▼──────────────────────────────────────────────┐
│                      JSON RESPONSE                                    │
│  {                                                                    │
│    "detections": [                                                   │
│      {                                                               │
│        "label": "Door",                                              │
│        "confidence": 0.95,                                           │
│        "bbox": {...center, width, height...},                       │
│        "distance_cm": 150                                            │
│      }                                                               │
│    ],                                                                │
│    "navigation_direction": "LEFT",                                  │
│    "processing_time_ms": 45                                         │
│  }                                                                   │
└──────────────────────┬──────────────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
    ┌───▼────────┐          ┌────────▼────┐
    │  Display   │          │ Voice TTS   │
    │  Arrows    │          │ "Turn Left" │
    │  + HUD     │          │             │
    └────────────┘          └─────────────┘


3. FLUTTER APP ARCHITECTURE
═════════════════════════════════════════════════════════════════════════════

          ┌─────────────────────────────────────────────┐
          │        FLUTTER APP (home_screen.dart)       │
          └────────────────┬────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
    ┌───▼────────┐   ┌─────▼──────┐   ┌──────▼──────┐
    │ Camera     │   │ Sensor Svc │   │ Voice Svc   │
    │ Service    │   │            │   │             │
    └───┬────────┘   └─────┬──────┘   └──────┬──────┘
        │                  │                  │
    ┌───▼──────────────────▼──────────────────▼────┐
    │                                               │
    │   STATE MANAGEMENT                           │
    │   ├─ _frameBuffer (camera frames)            │
    │   ├─ _detections (YOLO results)             │
    │   ├─ _targetHeading (door direction)        │
    │   ├─ _compassHeading (sensor direction)     │
    │   └─ _isBackendHealthy (connection status) │
    │                                               │
    └───┬──────────────────────────────────────────┘
        │
    ┌───▼──────────────────────────────────────────┐
    │                                               │
    │   UI LAYERS (Stack)                          │
    │   ├─ Camera Preview (bottom)                 │
    │   ├─ Navigation Arrow Widget                 │
    │   ├─ Sensor Navigation HUD                   │
    │   ├─ Debug Visualization                     │
    │   └─ Control Panel (top)                     │
    │                                               │
    └───────────────────────────────────────────────┘


4. BACKEND SERVICE ARCHITECTURE
═════════════════════════════════════════════════════════════════════════════

     ┌────────────────────────────────────────┐
     │   FLASK APPLICATION                    │
     │   (backend_service.py)                 │
     └────────────────┬───────────────────────┘
                      │
          ┌───────────┼───────────┐
          │           │           │
     ┌────▼──────┐ ┌──▼────────┐ │
     │ Endpoints │ │ Models    │ │
     └────┬──────┘ └──┬────────┘ │
          │           │          │
     ┌────▼────────────▼──────────▼──┐
     │                                │
     │  POST /detect                  │
     │  ├─ Load JPEG frame            │
     │  ├─ Run YOLO inference         │
     │  ├─ Filter by confidence       │
     │  ├─ Calculate distance         │
     │  ├─ Determine direction        │
     │  └─ Return JSON response       │
     │                                │
     │  GET /health                   │
     │  └─ Return server status       │
     │                                │
     │  GET /status                   │
     │  └─ Return model info          │
     │                                │
     └────┬───────────────────────────┘
          │
    ┌─────▼──────────────────────┐
    │   MODELS LOADED            │
    │  ├─ detect1892 (custom)    │
    │  │  ├─ 20 classes          │
    │  │  └─ 88.87% mAP          │
    │  └─ yolov8n.pt (COCO)     │
    │     ├─ 80 classes          │
    │     └─ Supplementary       │
    └────────────────────────────┘


5. WIDGET HIERARCHY (Flutter UI Tree)
═════════════════════════════════════════════════════════════════════════════

HomeScreen
├─ Scaffold
│  └─ Stack (main container)
│     ├─ CameraPreview (live feed) ← Bottom layer
│     │
│     ├─ Stack (detections overlay)
│     │  ├─ NavigationArrow
│     │  │  └─ Circle + Icon (arrow)
│     │  │     └─ Direction Label
│     │  │
│     │  └─ _buildDebugDetections()
│     │     └─ Bounding boxes for all objects
│     │
│     ├─ SensorNavigationHUD
│     │  ├─ Compass Ring
│     │  │  └─ Custom Painter (compass visual)
│     │  ├─ Direction Arrow
│     │  │  └─ Animated icon
│     │  ├─ Confidence Badge
│     │  └─ Info Bar
│     │     ├─ Tilt indicator
│     │     └─ Target heading
│     │
│     └─ Positioned (control panel) ← Top layer
│        ├─ Status text
│        ├─ Mute button
│        └─ Debug button


6. SERVICE ARCHITECTURE
═════════════════════════════════════════════════════════════════════════════

CameraService
├─ controlCamera()
├─ captureFrame()
├─ getFrameBuffer()
└─ Parameters: resolution, FPS

SensorNavigationService
├─ start()
├─ stop()
├─ setTargetHeading(degrees)
├─ calibrate()
├─ getCurrentHeading()
├─ Stream<NavigationState>
│  ├─ heading (0-360°)
│  ├─ direction (enum)
│  ├─ tiltAngle
│  ├─ confidence
│  └─ debugLabel
└─ Internal: Tilt compensation, smoothing

DoorVoiceCommandService
├─ speakOnce(direction, door)
├─ reset()
├─ clearAllHistory()
├─ Detection tracking by position hash
│  ├─ centerX, centerY
│  ├─ width, height
│  └─ distance_cm
└─ 3-second cooldown per door

VoiceService
├─ speak(text, identifier, cooldown)
├─ stop()
└─ FlutterTts integration


7. DETECTION PIPELINE TIMELINE
═════════════════════════════════════════════════════════════════════════════

Time(ms)    Stage
───────────────────────────────────────────────────────────────────────────
0           Camera captures frame (CameraX)
5           JPEG compression
10          Send to backend via HTTP
20          Backend receives & decompresses
25          Image preprocessing (resize to 640×480)
40          YOLO inference (GPU)
70          Confidence filtering
75          Distance calculation
80          Navigation logic
85          JSON serialization
90          Send response to app
130         App receives response
135         Parse JSON
140         Update UI state
150         Render frame buffer
160         Display on screen (one frame displayed)

TOTAL E2E: 150-160ms (ideal case)


8. REQUEST-RESPONSE FLOW
═════════════════════════════════════════════════════════════════════════════

app                          HTTP                        backend
 │                                                          │
 ├──── POST /detect ───────────────────────────────────────►│
 │     (JPEG image bytes)                                   │
 │                                                          │
 │                                                ┌─────────┤
 │                                                │ Process │
 │                                                │ YOLO    │
 │                                                └─────────┤
 │                                                          │
 │◄──── 200 OK ──────────────────────────────────────────┤
 │     {                                                    │
 │       "detections": [...],                             │
 │       "navigation_direction": "LEFT",                  │
 │       "processing_time_ms": 45                         │
 │     }                                                   │
 │                                                          │
 ├─── Parse JSON ─────────────────────────────────────────►
 │
 ├─── Update state (setState)
 │
 ├─── Render UI
 │    ├─ Draw bounding boxes
 │    ├─ Show navigation arrow
 │    ├─ Update compass heading
 │    └─ Speak TTS command
 │


9. TRAINING PIPELINE
═════════════════════════════════════════════════════════════════════════════

Data Preparation:
  Original Images (1,892)
         ↓
  Synthetic Dataset (6,204)
         ↓
  XML→OBB Conversion
         ↓
  Merged Dataset (8,985)
         ├─ train/  (7,896)  ← 88%
         ├─ valid/  (724)    ← 8%
         └─ test/   (365)    ← 4%

Model Training:
  YOLOv8n (pre-trained)
         ↓
  Remove detection head
         ↓
  Fine-tune on custom data
  • 30 epochs
  • Batch size: 64
  • Learning rate: initial 0.01
  • Optimizer: SGD
  • ✅ GPU: NVIDIA RTX A2000 12GB
         ↓
  Training complete (85 min)
         ↓
  Export to ONNX/PT
         ↓
  Deploy: best_model/best.pt

Results:
  ├─ mAP@0.5: 88.87% ✅ EXCELLENT
  ├─ mAP@0.5-0.95: 63.78% ✅ GOOD
  ├─ Precision: 87.5% ✅ FEW FALSE POSITIVES
  └─ Recall: 89.2% ✅ FEW MISSES


10. COORDINATE SYSTEMS
═════════════════════════════════════════════════════════════════════════════

Camera Frame Coordinates (Flutter):
  (0,0)───────────────► width
    │
    │   Door Detection
    │     (centerX, centerY)
    │
    height

Device Heading (Sensors):
  N
  │
  ├─ 0°
  │
  W───►E
    └─ 270°      90°
  │
  S
    └─ 180°

Navigation Direction:
  Door at (256, 240) in 640×480 frame
  Screen center: (320, 240)
  Offset: 256 - 320 = -64 (left side)
  → Direction = LEFT


═════════════════════════════════════════════════════════════════════════════
For more details, see PROJECT_README.md and INTEGRATION_GUIDE.txt
═════════════════════════════════════════════════════════════════════════════
